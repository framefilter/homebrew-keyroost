# homebrew-keyroost

Homebrew tap for [keyroost](https://github.com/framefilter/keyroost) — an
independent, vendor-neutral hardware-security-key manager.

```bash
brew tap framefilter/keyroost
brew install keyroost
```

`Formula/keyroost.rb` is rendered and pushed automatically by the release
workflow in the main repo on each tagged release.
